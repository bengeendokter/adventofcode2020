import * as fs from "fs";
import { splitByNewline, sum } from "./util.js";

export default function day7(): number
{
  console.log("day");
  const fileContent: string = fs.readFileSync("input/day7test.txt", "utf-8");

  

  return 0;
}
